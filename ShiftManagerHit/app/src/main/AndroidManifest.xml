<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shiftmanagerhit">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_schedule"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_schedule"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShiftManagerHit">
        <activity
            android:name=".NavigationDrawerActivity"
            android:label="@string/title_activity_navigation_drawer"
            android:theme="@style/Theme.ShiftManagerHit.NoActionBar"></activity>
        <!--        <activity-->
        <!--            android:name=".NavigationDrawerActivity"-->
        <!--            android:label="@string/title_activity_main_manu"-->
        <!--            android:theme="@style/Theme.ShiftManagerHit.NoActionBar" />-->
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>